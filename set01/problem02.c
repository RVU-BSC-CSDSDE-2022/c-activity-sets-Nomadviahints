#include <stdio.h>
 main()
{
  int w = 60;
  int x = 70;
  int y = 30;
  int a = (w + x + y);
  printf("Sum of %d, %d, %d is %d\n", w, x, y, a);
  int z = (w + x + y)/3;
   printf("Average of %d, %d,%d is %d", w, x, y,z); 
}
